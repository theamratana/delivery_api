<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <!-- Directory for log files (relative to project root when running locally) -->
    <property name="LOG_DIR" value="logs"/>

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} reqId=%X{reqId:-} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Daily rolling file appender with filename YYMMDD.log -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- No <file> property so the active log file matches the fileNamePattern for the current day -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Example: logs/251102.log for 2025-11-02 -->
            <fileNamePattern>${LOG_DIR}/%d{yyMMdd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} reqId=%X{reqId:-} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>